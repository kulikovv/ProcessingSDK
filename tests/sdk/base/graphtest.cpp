#include "graphtest.h"
#include <base/graph.h>
//#include "../../TestObjects.h"


GraphTest::GraphTest(QObject *parent) :
    QObject(parent)
{
}

void GraphTest::SubGraphTest()
{
   /* TestNode *node1=new TestNode();
    TestNode *node2=new TestNode();
    node1->setEnable(true);
    node2->setEnable(true);

    int* x = new int;
    x[0]=1;
    esp ptr = esp::init(x,1,"");
    base::Graph graph;

    graph.add(node1);
    graph.add(node2);
    graph.link(&graph,node1);
    graph.link(node1,node2);
    graph.receive(ptr);
    QCOMPARE(node2->getValue(),3);*/
  //  graph.link(&node2,&graph);



}
